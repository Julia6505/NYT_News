<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand">NYT Mongo Scraper</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a class="nav-item nav-link active" href="#">Home <span class="sr-only"></span></a>
      <a class="nav-item nav-link active" href="#">Saved</a>
      <button type="submit" class="btn btn-secondary" id="submit-scrape" data-toggle="modal" data-target="#myModal">Scrape New Articles</button>
    </div>
  </div>
</nav>
  <div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-3">The New York Times</h1>
    <p class="lead">Mongo Scraper</p>
  </div>
  </div>
<section>

<div class="modal" id="myModal" tabindex="-1" role="dialog">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Cool!</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <p>You just scraped some New York Times tech articles!</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" id="show-articles" data-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>

<div class="card">
<div class="card-body"></div>
   {{#each articles}}
    <h4 class="card-header">{{this.headline}}</h4> 
    <h4 class="card-title">{{this.link}}</h4>
    <p class="card-text">{{this.headline}}</p>
    <div>
    <button class="btn btn-primary">Save article</button>
    </div>
    {{/each}}
    
  </div>
</div>
  
</section>